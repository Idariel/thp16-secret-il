<h1>What is the answer to the most important question about the universe?</h1>
<h3>From Douglas Adams's books (forget about Dr Who)</h3>
<br />

<p><%= link_to 'First, create new user', new_user_path %></p>
<p><%= link_to 'Want to know about the secret page', new_user_path %></p>
<b />

<table>
    <th><h3>List of User</h3></th>
    <th colspan="3"></th>
  </tr>

  <% @users.each do |user| %>
    <p><tr>
      <td width="300px"><%= user.email %></td>
      <td width="50px"><%= link_to 'Show', user_path(user) %></td>
      <td width="50px"><%= link_to 'Edit', edit_user_path(user) %></td>
      <td width="50px"><%= link_to 'Delete', user_path(user),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr></p>
  <% end %>
</table>
