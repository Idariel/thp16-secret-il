<%= render 'header' %>

<h2>User</h2>

<table>
  <tr>
    <th>User Mail</th>
    <th colspan="3"></th>
  </tr>
  <tr>
    <td width="250px"><%= @user.email %></td>
    <td width="50px"><%= link_to 'Show', user_path %></td>
    <td width="50px"><%= link_to 'Edit', edit_user_path %></td>
    <td width="50px"><%= link_to 'Delete', user_path,
            method: :delete,
            data: { confirm: 'Are you sure?' } %></td>

  </tr>
</table>

<%= render 'footer' %>
